---
description: Show RunVouch status of all scheduled agents and open alerts
---
Run `rv status` and `rv alerts` (fall back to the runvouch MCP tools if rv is not installed) and present a short table: agent, state, last run, cost 24h, open alerts — then list open alerts with the most likely cause and one next step each.
